---
sidebar_position: 6
title: Screenshot comparison test
---
import VariationBadge from '@site/src/components/VariationBadge';

# Screenshot comparison test

This page explains `maxVariation`, the Doc Detective option that compares a new screenshot against the one already saved from the previous run, and shows the live results from the two tests it's wired into.

## Why this matters

Every other screenshot test on this site captures an image and either shows it unconditionally or checks that something rendered. None of them check whether today's capture looks like yesterday's. `maxVariation` closes that gap, applied here to two screenshots with very different expectations: the [API reference test](/showcase-site/doc-detective/api-reference-test)'s screenshot, which should almost never change, and the [doodle test](/showcase-site/doc-detective/doodle-test)'s screenshot, which changes on purpose whenever Google ships a new doodle. Comparing both shows what a pixel diff actually measures — a real regression signal on a stable page, and a genuinely fluctuating number on a page that's supposed to change.

## Latest result

**API reference** — a rendered API spec, not day-to-day content. A second, `aboveVariation`-gated capture (`api-reference-baseline.png`, 2% threshold) sits alongside the existing display screenshot. A WARNING here is a real signal that something in the render shifted.

<VariationBadge dataFile="/doc-detective/api-reference-variation.json" label="API reference" />

**Doodle** — compared against yesterday's capture at a 10% threshold. Most days that's near zero; on days Google ships a new doodle it's large. Neither outcome is a failure.

<VariationBadge dataFile="/doc-detective/doodle-variation.json" label="Doodle" />

## How the test works

This isn't a standalone test with its own steps — it's the same `screenshot` step option, `maxVariation`, applied to a step already defined on each of the two pages above. A Doc Detective `screenshot` step can take:

- **`maxVariation`** — the fraction of pixels (0 to 1) allowed to differ between a new capture and the image already saved at that path, before the comparison is flagged. Defaults to 0.05 (5%).
- **`overwrite`** — what to do with the existing file. There are three modes, and they behave less alike than the names suggest:
  - `"true"` replaces the file unconditionally and runs **no comparison at all**. This is what the API reference page's main display screenshot still does.
  - `"false"` captures once. On every later run, if a file already exists at that path, the step is **skipped outright** — no new capture, no comparison. It freezes a reference image; it doesn't repeatedly check anything against it.
  - `"aboveVariation"` is the only mode that actually compares on every run: it captures, diffs against the existing file, and replaces the file only if the difference exceeds `maxVariation`. This is what both comparisons above use.

## What makes this test fail

Neither comparison can turn a badge red. Exceeding `maxVariation` reports **WARNING**, not FAIL — a deliberate severity choice in Doc Detective, not a gap in this site's setup. "This looks different" is information, not necessarily breakage, and the status badges on this site treat WARNING the same as PASS.

## Run the test manually

There's no separate test file for this page. See [the API reference test](/showcase-site/doc-detective/api-reference-test#run-the-test-manually) and [the doodle test](/showcase-site/doc-detective/doodle-test#run-the-test-manually) for how to run each comparison locally.
